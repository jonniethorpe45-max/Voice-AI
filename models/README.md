# Models

Place model assets and registries here:

- `demucs/` (optional separated checkpoints)
- `svc/` (pretrained voice conversion checkpoints)
- `registry.json` (model metadata)

The MVP runs with lightweight algorithmic fallbacks when model files are missing.
