## Services

This folder stores service-level architecture notes and production deployment guidance.

- `backend`: FastAPI API service
- `worker`: asynchronous DSP/AI processing worker
- `redis`: job queue and status store

